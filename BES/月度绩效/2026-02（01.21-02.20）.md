#### 工作量和工作质量

1. 功能新增或同步：
	1) BESAPPENT-2247  BES11.0.1 企业版：自定义属性支持可编辑
	2) BESAPPENT-2247  BES11.0.1 标准版版：自定义属性支持可编辑
	3) BESAPPLITE-834 BES9.5.2 lite版，增加自定义属性createOnInitialize, 在Server启动时建立连接
	4) BESAPPENT-2397 从10.0.3同步事务服务功能
	5) BESAPPENT-2392 11.0.1 添加DMS补丁版本与节点版本的一致性校验
	6) BESAPPENT-2392 9.5.5. 添加DMS补丁版本与节点版本的一致性校验
	7) BESAPPLITE-840  952springBoot jdbc参数调整
		1) 1. server.bes.jdbc.validation-query-timeout 默认配置为30s   100%
		2) server.bes.jdbc.min-evictable-idle-time-millis 默认配置改为-1 100%
		3) server.bes.jdbc.min-idle 默认配置改为8  100%
    
4. server.bes.jdbc.time-between-eviction-runs-millis  支持可配置  100%
    
5. server.bes.jdbc.enableSharingResources 支持可配置 100%
	1) BESAPPLITE-840  955springBoot jdbc参数调整
		1) 1. server.bes.jdbc.validation-query-timeout 默认配置为30s
		2) server.bes.jdbc.min-evictable-idle-time-millis 默认配置改为-1
		3) server.bes.jdbc.fast-fail-validation 支持可配置
		4) server.bes.jdbc.min-failure-count-to-suspend 支持可配置
		5) server.bes.jdbc.async-close-connection 支持可配置
	
3. BUG修复：
	1) BESAPPENT-2348  BES9.5.5企业版 部署连接器应用失败, config_text 字段长度不够
	2) BESAPP-23168  BES9.5.5.1：无法使用LicenseManager
	3) BESAPPENT-2362 ：BES955企业版：提供WebService开关
	4) BESAPP-23259： BES9.5.2 : traceResultSet、tracestatement 默认使用弱引用
	5) BESAPP-23248： BES10.0.2: traceResultSet、tracestatement 默认使用弱引用
	6) BESAPP-23258：BES11.5：traceResultSet、tracestatement 默认使用弱引用

4. 现场问题处理：
	1) Springboot9.5.5 启动webflux应用失败
	2) Springboot9.5.5 启动License信息没有打印补丁信息 


#### 工作积极主动性

工作积极主动，无消极情况。

#### 制度遵守

符合公司管理制度遵守和团队管理要

#### 团队合作

能够积极与团队成员正面的沟通协调互助，如协助测试解释测试步骤，BUG缺少附件时积极沟通，与其他开发积极沟通修改内容及协助等

#### 技能提升

1. 对于命令行的运行机制更加熟悉。了解命令行是如何通过脚本的方式一步步调用脚本，确定其对应的Command。
2. 对于部署应用的逻辑更加了解，了解应用部署过程中的一些基本操作以及不同应用之间部署的区别等等
3. 代码规范方面更加注意。
4. 与同事沟通能做到更加高效，通过文档把思路以及方案呈现出来，实现更高效的沟通