#### 工作量和工作质量

1. 功能新增或同步
	1) BESAPP-22790 从11.0.1同步实例健康功能到9.5.7版本
	2) BESAPP-22824 （兴业银行）适配Skywalking agent 8.7.0和9.1.0
	3) BESAPPENT-2242  BES11.0企业版：连接器应用支持编辑资源适配器属性
	4) BESAPP-22995 BES11.0：连接器应用支持编辑资源适配器属性
2. BUG修复：
	1) BESAPPENT-2211 BES10.0.3企业版：控制台测试 - 集群信息，设置健康检查统治保存后，没有生效
	2) BESAPP22957 BES10.0.3标准版：控制台测试 - 编辑管理对象资源对于之前phyicalname为空的，填写内容保存会报错
	3) BESAPPENT-2218 BES10.0.3企业版：控制台测试 - 使用控制台补丁管理上次T补丁，引用补丁后，启动节点报错
	4) BESAPP-22912 ：BES10.0.3标准版，类加载试图没有实现排序功能，也无法指定某个jar优先加载
	5) BESAPPLITE-760 ：BES9.5.5 支持 max-swallow-size 配置
	6) BESAPPLITE-767  BES9.5.5：新增支持通过 yml 配置系统属性
	7) BESAPPLITE-761  BES9.5.5：DevTools 因文件变更触发重启，BES servlet 容器共享机制，在关闭后未重置其共享标志
	8) BESAPPLITE-764  BES9.5.5SpringBoot：支持关闭启动时类冲突检测
	9) BESAPP-22967 Springboot955 引用 spring-web5.3.0 版本以上出现 noSuchMethod:logger 错误
	10) BESAPPLITE-768 BES10.0.2 引用 spring-web5.3.0 版本以上出现 noSuchMethod:logger 错误
	11) BESAPPLITE-769 BES11.5.0 引用 spring-web5.3.0 版本以上出现 noSuchMethod:logger 错误
	12) BESAPPENT-2233  BES10.0.3企业版：控制台测试-应用列表，进行过滤部署状态，失败，请修改
	13) BESAPPLITE-772  BES11.5 springboot ：支持配置docbase所在目录
	14) BESAPPENT-2240  BES11.0.1 企业版：控制台测试 - 应用列表，进行过滤部署状态，失败，请修改
	15) BESAPPLITE-766  BES9.5.5 SpringBoot WebFlux应用支持指定 docbase 目录
	16) BESAPP-23021  BES957 IF00012补丁，实时监控，连接泄露场景下关闭连接，“操作成功”信息会把上面的监控页签覆盖
	17) BESAPP-23007 BES957 IF00012补丁，实例健康状态获取不到http等待任务数，请修改
	18) BESAPP-23023  BES957 IF00012补丁，实时监控，线程池测试死锁，强停死锁会把上面的监控页签都冲掉
	19) BESAPP-23020  BES957 JDK使用17是，控制台登录页面的验证码刷新不出来，切换回8就可以了
	20) BESAPP-23026 BES 9.5.5:038补丁，去除iastoolpassport之后，为什么create --password-file 还是需要传入 iastoolpassport
	21) BESAPPENT-2184  BES9.5.5.1企业版：WEB容器-HTTP监听器。新建http监听器，输错后重新修改保存失败
	22) BESAPPENT-2226  BES9.5.5.1企业版：组件管理-高可用组件，启动/停止后，页面闪一下，无报错也没有提示信息
	23) BESAPPENT-2177  BES9.5.5.1企业版：实例监控-监控回放-JDBC资源，点击后页面加载空白，菜单不能点击需要重新刷新页面才正常
	24) BESAPPENT-2179  BES9.5.5.1企业版：日志服务-常规配置-日志属性，添加配置 fileDateFormat=yyyy-mm-dd HH-mm-ss-SSS, 实例日志目录下输出的日志文件带引号


#### 工作积极主动性


#### 制度遵守

符合公司管理制度遵守和团队管理要

#### 团队合作

能够积极与团队成员正面的沟通协调互助，如协助测试解释测试步骤，BUG缺少附件时积极沟通，与其他开发积极沟通修改内容及协助等

#### 技能提升

1. 新技能学习：
	1) Jenkins基本学习及使用。
	2) Git相关： Squash Commits、Amend Commit、recurse-submodules
	3) IDEA Remote Debug
2. 工作思路转变：由之前的业务开发逐渐往项目思维转变。并通过逐行调试熟悉不同于微服务体系下的调用关系，而是通过进程执行命令的方式去调用。
3. 编码常见埋坑点：Boolean 为包装类型，起getter方法格式只能是getXXX()。boolean为基本类型，其getter方法可以为isXXX()，如果是 Boolean 类型的字段写成 isXXX(), freemarker获取字段时则娶不到