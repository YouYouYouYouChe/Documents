#### 工作量和工作质量

1. 功能新增或同步：
	  BESAPPENT-2287  BES9.5.5 企业版：可将附加类库在界面配置，无需去应用 WEB-INF 下面操作
	
2. BUG修复：
	1) 1. BESAPPENT-2261  BES955企业版：给主机设置环境变量之后，只重启实例无法读取到该环境变量，必须要重启节点后再启动实例才能读取到
	2) 1. BESAPPENT-2259 BES955 集群lsof -i :11900 监听存在其他连接，导致dms一直误认为实例启动，从而启动失败
	3) BESAPPENT-2281  BES 11.0.1企业版：监控诊断，下载类分析报告报错 
	4) BESAPPENT-2283  BES 10.0.3企业版：监控诊断，下载类分析报告报错  
	5) 1. BESAPPENT-2279  BES955企业版，模板管理 -> 节点模板 -> 日志服务，只有第一次保存才能成功
	6) BESAPPLITE-789  BES11.0.1轻量版，导入包含非web的其他功能模块的通用型许可后，会报错无法使用
	7) BESAPPLITE-506  11.5.0 ：产品启动最低版本改为JDK17
	8) BESAPPENT-1974  9.5.5企业版： CM_RESOURCE_REF 表里的JDBC连接池密码是非加密存储的
	9) BESAPP-22770  10.0.2 标准版： 打到11补丁，iastool list --user 出现 JsonSyntaxException
	10) BESAPPENT-2150  BES955集群版，node jmx_port 和 instance jmx_port 不要重复
	11) BESAPPENT-2150  BES10.0.2集群版，node jmx_port 和 instance jmx_port 不要重复
	12) BESAPPENT-2150  BES10.0.3集群版，node jmx_port 和 instance jmx_port 不要重复
	13) BESAPPENT-2150  BES11.0.1集群版，node jmx_port 和 instance jmx_port 不要重复
	14) BESAPP-22708  BES 11.0.1标准版：用户管理页面，存在多用户是，排序的顺序不友好
	15) BESAPP-22766   BES9.5.5标准版 增量更新支持CLI
	16) BESAPP-22766   BES11.0.1标准版 增量更新支持CLI
	17) BESAPP-13162   BES11.0.1标准版：关闭1900的https后，使用命令行操作报错
	18) BESAPPENT-2163  BES9.5.5.1企业版：实例配置开启SNMP后，实例无法启动
	19) BESAPP-23130  BES10.0.3/11.0.1 标准版，验证码使用 思源黑体 无法加载字体


#### 工作积极主动性

工作积极主动，无消极情况。

#### 制度遵守

符合公司管理制度遵守和团队管理要

#### 团队合作

能够积极与团队成员正面的沟通协调互助，如协助测试解释测试步骤，BUG缺少附件时积极沟通，与其他开发积极沟通修改内容及协助等

#### 技能提升

	